import { UpdateValDirective } from './update-val.directive';

describe('UpdateValDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateValDirective();
    expect(directive).toBeTruthy();
  });
});
